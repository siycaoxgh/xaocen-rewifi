# XAOCEN ReWiFi 更新日志

## v2.3 · 2026-09-14 · 当前发布

- 修复正式发布构建错误地把隐私选项绑定到测试模式的问题；“增强匿名分析”“匿名崩溃报告”和“禁止所有统计上传”现在均可在正式版设置页操作。
- 保留“禁止所有统计上传”的总开关逻辑：启用总开关时，两个可选统计项暂时禁用；关闭总开关后恢复选择。
- 账号登录增加“取消登录”按钮；用户在浏览器切换账号时，可先取消旧设备授权请求并重新发起登录。
- 关闭账号中心会立即取消正在进行的设备授权轮询，避免窗口关闭后仍有授权任务等待或异常回调。
- 根据前一版真机反馈重新分配账号、网络、帮助、隐私区域高度，并增加隐私控件行高，避免文字和选项被遮挡。
- 再次按真机截图重构两个窗口：默认尺寸调整为 1040×900，允许用户调整大小和最大化；窗口超过当前屏幕工作区时自动缩小，内容不足时使用滚动条。
- 设置页“打开账号中心”移入正常表格行，不再以悬浮坐标覆盖分组标题；顶部介绍、网络开关和按钮行重新预留 DPI 缩放空间。
- 账号中心拆分为“账号会话”和“登录与设备说明”两个独立区域，按钮行和说明文字不再与固定高度互相覆盖。
- 删除容易误解的“撤销此设备”按钮和当前产品不再使用的撤销适配；客户端只保留清晰的“退出账号”，其他设备管理和账号注销统一交由 Account 网页处理。
- 将“打开账号中心”放入账号会话卡片底部，删除单独占用整行的顶部按钮栏；保存按钮固定在底部操作栏并增加可点击面积。
- 设置页将窗口剩余高度分配给隐私与统计区域，确保底部数据范围说明完整显示。
- 账号中心改为应用级单例：设置页、托盘菜单和连续点击均复用并置顶同一个窗口，不再创建两个账号中心。
- 修复窗口仅能横向适配、纵向缩小时直接裁切的问题：设置页和账号中心现在使用独立滚动视口与最小内容高度，右侧纵向滚动条会按需出现和消失。
- 纵向缩小时不再压缩账号操作按钮、帮助区域、隐私选项或保存按钮；用户可滚动访问完整内容。
- 增加账号按钮区底部内边距并扩大状态卡片高度，避免登录、取消登录和退出账号的边框贴近分组边界或被遮挡。
- 使用 WinForms 实际渲染检查账号中心：三个账号按钮均为 36 像素高，所在操作区可用高度为 56 像素，确认新版源码没有越界或被相邻分组裁切。
- 统一产品帮助、用户协议和隐私说明的在线判断：三者共用 6 秒官网可用性检查，成功时打开在线页面，失败时回退对应内置副本。
- 已验证官网产品页、用户协议、隐私说明和反馈入口均返回 HTTP 200；此前产品帮助和用户协议打开本地副本，是客户端 2.5 秒检查超时造成，而非官网页面缺失。
- 用户协议和隐私说明顶部移除各自不同的渐变色，统一为与本地产品介绍一致的纯白卡片背景；保留本次人工调整的协议正文和页脚内容。
- 发布前逻辑复查修复恢复流程在退出或升级中断时可能让 Wi-Fi 网卡停留在禁用状态的问题，并为取消中的 `netsh` 子进程增加终止保护。
- 账号退出改为本机优先：即使 Account 服务或网络暂时不可达，也会清理当前 Windows 用户的刷新凭据；账号中心和设置页同步显示最终状态。
- 修正开机启动关闭操作的幂等结果，以及有效任务因电源参数加固失败而被界面误报为“未启用”的问题。
- 旧离线授权、离线签名和二维码源码不再参与 v2.3 编译；Account 设备登记公钥改由独立身份服务生成，并沿用原凭据目标保证升级后的设备身份稳定。
- “GitHub 更新页面”改为打开 Releases 页面；脱敏诊断增加文件版本和内部构建号，便于区分同为 2.3 的不同便携包。

- 保留跨版本全局单实例锁，并增加当前 Windows 用户范围内的安全进程通信通道。
- 重复打开同版本或较旧版本时，主动唤醒已有设置或账号窗口，不再只依赖可能被遮挡的普通提示框。
- 新版启动时如发现支持新协议的旧版正在运行，会显示当前版本、新版版本和新版路径；用户确认后旧版正常退出，新版等待并继续启动。
- 对 2.0.2 及更早版本提供前台兼容提示，指导用户先从系统托盘退出后重新打开新版。
- 更新提示提供“打开新版位置”，明确便携版不会扫描或静默删除其他位置的旧 EXE。
- 设置窗口改为单一可复用实例，重复请求只唤醒现有窗口。
- 修复升级交接时托盘图标已经消失、旧进程却仍占用单实例锁的问题：退出清理完成后才隐藏托盘，并在正常退出超过 10 秒时结束自身，确保新版能在 20 秒等待期内接续启动。
- 文档入口精简为“产品介绍与使用帮助”，在线时打开官网产品页，无法联网时打开本地副本；移除托盘和设置页重复的在线/本地按钮。
- 首次启动说明补充“请先阅读用户协议和隐私说明。继续使用后，将打开产品介绍与使用帮助。”协议按钮同样采用在线优先、失败回退本地。
- 统一内置产品介绍、用户协议和隐私说明的页面框架、字体、色彩、内容卡片、间距和页脚布局。
- 本地文档移除官网、GitHub 和本地页面跳转链接，仅保留必要的隐私联系邮箱和离线说明。
- 增加自动构建号：同一对外版本的不同构建可被单实例升级流程识别，构建号不在普通界面显示，仅用于内部日志和版本比较。
- 当前正式便携包：`build/v2.3-release-20260914-r7/XAOCEN-ReWiFi.exe`；文件版本 `2.3.0.0`，大小 `56,555,844` 字节（约 53.94 MiB），SHA-256：`B01D0FE74CEBE6FFD498ECB73D49D2FDC503F9C50F0D3ED4FBA11C6933A7A855`；内部构建号 `639249667889753231`，普通界面不显示该号码。
- 对外版本统一采用两段式编号 `2.3`，文件和程序集版本统一为 `2.3.0.0`。
- 版本、内置协议、README、精简验收和升级说明同步更新为 2.3。
- 历史 v2.3 构建目录已清理；仍被当前运行进程占用的旧目录暂时保留，历史校验值不再作为当前下载地址。

## 历史版本记录（以下内容不代表当前 v2.3 功能）

### v2.0.2 · 2026-09-13

- 可执行文件改名 XAOCEN-ReWiFi.exe，保留配置、凭据、产品编号和单实例名称。
- 增加产品专属用户协议、隐私说明及内置 HTML 副本。
- 首次及本协议版本首次展示说明先于统计初始化，支持禁止所有统计上传，既有偏好保留。
- 首次产品文档保持在线优先/离线本地；设置和托盘增加在线协议入口并回退本地。
- 官网源码增加 /products/rewifi/terms/ 与 /products/rewifi/privacy/，修正旧离线激活文案；尚未部署。
- 普通版本实际已启用生产统计，隐私正文依据当前源码与官网政策撰写；纠正此前未启用生产统计的旧说明。
- v2.0.2 构建包已清理，路径仅作为历史记录保留。


### v2.0.1 · 2026-09-13

- 永久免费，账号登录可选；无账号、离线或会话过期均不阻止网络恢复。
- 账号中心替代授权中心，移除会员、期限、离线授权和摄像头扫码入口。
- 停止启动离线校验及登录后的权益查询，保留协议要求的设备登记和凭据保护。
- 移除 OpenCV、QRCoder 发布依赖，继续自包含便携发布。
- 保留旧配置、刷新凭据和历史离线文件，不干扰正在运行的旧版。
- v2.0.1 构建包已清理；验证结果和体积基准保留在 `精简计划与验收.md`。


### v2.0 遥测状态与离线摘要修正版 · 2026-09-04

- 修复正式发布构建未启用遥测端点的问题；发布构建现在向 `https://auth.xaocen.studio/v1/telemetry/events` 发送匿名运行统计，`TelemetryTest=true` 继续保持测试环境隔离。

- 将设置窗口和“XAOCEN ReWiFi 授权中心”统一为 1040×810，并统一微软雅黑字体、边距、按钮高度、应用图标和品牌色。
- 授权中心在线账号与离线授权使用等宽双栏和固定中间间隔；字段标签扩展至 96 像素，避免“当前状态”“会话信息”等文字断行。
- 调整授权详情、说明文字和按钮区的行高，改为内容从上向下排列、底部使用弹性空白；在线说明、离线说明、授权文件位置和全部按钮均可完整显示。
- 设置窗口与授权中心启用 DPI 自动缩放和滚动回退，降低不同 Windows 显示比例下的裁切风险。
- 删除设置页离线授权摘要中的“权益到期”项；当前离线授权签发不保证提供 `entitlementExpiresAt`，避免把“未单独设置”误解为授权异常。
- 保留设置页在线 XAOCEN Account 权益有效期，以及离线授权的联网检查模式、下次联网检查和最迟重新授权信息。
- 增加遥测状态变更通知：待上传数量在事件入队、清空或后台发送完成后自动刷新，避免设置页显示旧队列数量。
- 遥测服务部分拒绝事件时，仅将服务端返回的拒绝错误码及数量写入运行日志，不记录事件内容或匿名标识，并按协议丢弃被拒绝事件。
- 已构建统一 UI 测试包（已清理）：原文件版本 `2.0.0.0`，大小 `92,271,455` 字节，SHA-256 为 `F5032AD384FC80C0E592C02EB8CA8C39574DDAD04CB1481A1F669E2895079C37`。

### v2.0 遥测测试接入 · 2026-09-04

- 增加不依赖 Node.js 的 C# 遥测适配层，复用 XAOCEN Telemetry API 1.0 的事件信封和字段白名单。
- 增加匿名实例 ID、会话 ID、首次运行、启动、版本更新、核心功能激活、授权检查、功能使用和脱敏崩溃事件。
- 将基础运行统计改为默认启用，并与增强匿名分析、匿名崩溃报告分层控制；增强分析和崩溃报告分别可选，崩溃报告默认关闭。
- 增加“禁止所有数据上传”总开关；启用后不生成、不缓存、不发送任何遥测事件，并清理本地待上传队列，不影响 Wi-Fi 恢复、账号授权或离线运行。
- 基础事件仅发送协议要求的最小字段，不上传 Windows 版本、架构、通道、网络或设备详情。
- 压缩设置页顶部留白并缩小产品图标，将“打开授权中心”移动到账号卡片右上方，同时为隐私设置第三行保留固定高度，避免总开关和重置按钮被遮挡。
- 修正设置页二次布局：账号区完整显示离线授权日期信息，网络恢复区域不再占用多余空白，基础统计范围说明改为独立底部行并完整显示。
- 增加独立本地遥测目录、断网队列、批量发送、429/5xx 重试、413 拆包和服务端 `eventId` 去重处理。
- 增加 UI、主线程、未处理异常和后台任务异常捕获；崩溃信息上传前清理令牌、邮箱、路径、IPv4/IPv6、MAC、SSID 和网卡名称，并限制字段长度。
- 历史 v2.0 测试构建的上传地址只在 `TelemetryTest=true` 时启用；当前 v2.3 正式包已固定使用生产遥测入口，测试构建不得分发。
- 已构建布局修正版（已清理）：原文件版本 `2.0.0.0`，大小 `92,269,407` 字节，SHA-256 为 `79113B120DC16666A3C6A131681B8D714A88ECDF781CEAACEDD8E5B26CA5529B`。
- 详细接入边界、数据位置、测试命令和验收清单见 `遥测接入与隐私说明.md`。

### v2.0 · 2026-09-02

- 将应用、程序集、文件属性和应用清单版本统一升级为 `2.0.0.0`。
- 保持在线账号授权与离线授权在同一个客户端版本内共存：网络可用时优先 Account 在线检查，网络不可用时自动使用本地签名校验。
- 保留 XAOCEN Account 登录、刷新、退出、撤销、权益查询、离线授权文件、公钥二维码和授权二维码扫描能力。
- 优化账号与离线授权信息展示，明确区分账号会话、离线授权文件、联网校验、权益到期、下次联网检查和最迟重新授权日期。
- 统一沿用 `%LOCALAPPDATA%\\XAOCEN ReWiFi` 和 Windows Credential Manager 的现有存储位置，升级客户端不会清空 v1.7/v1.8.x 数据。
- 清理项目内历史构建产物，仅保留新的 v2.0 发布包。
- v2.0 发布包已清理；原文件版本 `2.0.0.0`，大小 `92,229,983` 字节，SHA-256 为 `70BCA7368495F12DB01ED891AEF8B32DBC672E65C6A5CCEF0B2C4D3567F403B4`；Release 发布通过，0 个警告、0 个错误。

## v1.8.1 · 2026-09-01（历史版本）

- 精简托盘和设置页的重复入口：自动选择文档保留为首次启动逻辑，托盘仅在“文档与帮助”中提供在线文档和本地文档。
- 移除设置页重复的产品介绍、自动选择文档和旧版授权按钮；账号卡片统一通过“打开授权中心”进入。
- 保留托盘顶部产品名称、版本、状态、立即恢复、自动恢复和开机启动不变，并继续将反馈、脱敏诊断和运行日志分组到“帮助与反馈”。
- 明确区分“XAOCEN Account 已连接”和“离线授权在线校验通过”：前者代表账号会话，后者代表离线授权文件和设备状态通过服务端检查，在线校验不会隐式登录账号。
- 最近一次离线授权检查结果同步到授权中心和设置页，在线检查、本地回退、拒绝和未配置状态均显示具体模式与说明。
- 优化设置页账号卡片排版：加宽字段标签，分行显示在线校验、在线有效期和离线有效期，避免状态文字挤压或断行。
- 再次调整账号卡片为“账号会话 / 离线授权”双栏摘要，使用独立信息面板，避免中文标签和日期在高 DPI 下换行。
- 设置页订阅账号会话与离线授权状态变化，后台会话恢复或联网检查完成后自动刷新；授权中心重新打开时沿用最近一次检查结果。
- 补充显示离线授权的权益到期、下次联网检查和最迟重新授权日期；未设置权益到期时不再误显示为“未知”。
- 补充 v1.7 至 v1.8.x 的配置、离线授权和 Credential Manager 凭据继承规则。
- 精简自包含发布包：目标框架改为 `net10.0-windows`，移除不必要的固定 Windows SDK 运行组件，保留 OpenCV 摄像头二维码扫描能力，发布体积较上一版减少约 6 MB。
- 设置页继续显示独立任务栏图标，账号与离线授权使用醒目的独立卡片。
- 版本元数据、应用清单、产品介绍、README 和接入联调文档统一更新为 `1.8.1`。
- v1.8.1 状态同步修正版的本地构建文件已在 v2.0 发布前清理；版本校验信息保留在历史记录中。

## v1.8 · 正式版（历史版本）

- 托盘顶部的产品名称、版本、状态、立即恢复、自动恢复和开机启动保持不变。
- 将托盘文档入口折叠为“文档与帮助”，将反馈、脱敏诊断和运行日志折叠为“帮助与反馈”。
- 增加托盘“账号与离线授权”直达入口。
- 重排设置窗口，增加醒目的“账号与离线授权”独立卡片，显示 XAOCEN Account 状态、在线/离线授权状态和授权有效期。
- 增加设置页面“帮助与文档”区域，集中提供在线文档、本地文档、问题反馈和运行日志入口。
- 设置窗口显示独立任务栏图标，支持从其他窗口后方快速找回；关闭设置后仅保留系统托盘图标。
- v1.8 正式发布包已在 v2.0 发布前清理；原文件版本 `1.8.0.0` 和 SHA-256 校验记录保留在 Git 历史中。

## v1.7 · 正式版（历史版本）

- 正式接入 XAOCEN Account 在线账号授权、会话恢复、刷新、退出、设备撤销和 ReWiFi 权益查询。
- 保持统一授权策略：网络可用时优先在线检查，网络不可用时自动回退到本地离线签名校验。
- 正式接入离线授权文件、`compactLicense` 校验、设备公钥二维码和授权二维码扫描。
- 访问令牌仅保存在内存，刷新令牌和离线设备私钥仅保存在 Windows Credential Manager。
- 移除开发测试专用的“强制使用本地校验”入口和客户端“开发测试”标记。
- 更新本地产品介绍、账号与离线授权说明、编译发布路径和正式版本信息。
- v1.7 正式发布包已在 v2.0 发布前清理；原文件版本 `1.7.0.0` 和 SHA-256 校验记录保留在 Git 历史中。

## v1.6 · 开发测试（历史版本）

- 增加 XAOCEN Account 设备授权适配层，使用 `productId=rewifi` 和 `platform=windows-x64`。
- 增加访问令牌仅驻留内存、刷新令牌写入 Windows Credential Manager 的安全边界。
- 增加在线会话恢复、刷新令牌轮换、退出和撤销接口适配。
- 增加离线设备 Ed25519 密钥生成、Credential Manager 私钥保存和 `primary` 公钥内置验证。
- 增加 `compactLicense` 的规范化 JSON、Base64URL 和 Ed25519 签名校验。
- 增加离线授权文件导入，并在同一版本内按“在线优先、网络失败回退本地”策略检查授权。
- 将账号和离线授权操作集中到独立授权中心，增加在线会话、离线授权和网络回退状态展示。
- 增加在线设备授权轮询进度展示：明确显示“等待账号批准”、授权截止时间、上次/下一次轮询时间、当前轮询次数和授权剩余时间。
- 在线授权成功后明确显示“XAOCEN Account 已连接”，避免状态长期停留在笼统的“处理中”或“授权中”。
- 在线账号会话建立或恢复后接入 `GET /v1/account/entitlements`，显示 ReWiFi 权益状态、类型、有效期、设备占用和离线授权资格。
- 设备授权请求按协议提交 `productId=rewifi`、`platform=windows-x64` 和客户端设备名称。
- 增加设备公钥二维码生成和 Account 离线授权二维码扫描功能；二维码内容分别使用原始 SPKI PEM 公钥和原始 `compactLicense`，并继续通过本地授权校验。
- 增加摄像头不可用、二维码类型不匹配和二维码读取失败提示，保留字符串粘贴与授权文件导入备用路径。
- 已完成二维码真机测试和低像素电脑摄像头验证；v1.6 测试发布包已在 v1.7 正式发布前清理。
- 针对低像素电脑摄像头优化二维码传输：Account 授权二维码调整为 `640px`、更大静区和低密度纠错级别；ReWiFi 扫描端增加灰度、二值化和放大识别回退；本机公钥二维码显示区域扩大。
- 增加 compactLicense 字符串粘贴导入和开发测试强制本地校验入口。
- 增加 ReWiFi 与 XAOCEN Account 项目分工、二维码协议依赖和真机联调清单。
- 将反馈入口切换至 XAOCEN 统一支持页面，并自动标记 `product=rewifi`。
- 增加在线产品文档不可达时回退本地文档的处理。
- 增加 GitHub 问题反馈入口和脱敏诊断信息复制入口。
- v1.6 的账号和离线授权入口仅用于开发测试；相关能力已在 v1.7 完成正式版切换。
- 已通过 Account API 提供的有效、篡改、过期和错误产品编号测试向量验证离线校验逻辑。
- 已完成真实设备断网、在线回退、二维码、撤销和授权状态端到端验收；本节内容仅保留历史开发记录。
- v1.5 归档版保持不变。

## v1.5 · 2026-08-22

- 修复 `WifiController.IsAdministrator()` 在正式发布包中缺少 `System.Security.Principal.Windows` 程序集、导致自动恢复流程异常的问题。
- 固化 `win-x64` self-contained 单文件发布配置，并核对正式发布包的依赖完整性。
- 修复连通性探测继承本机代理的问题，探测现在绕过系统代理、`HTTP_PROXY`、`HTTPS_PROXY` 和 `ALL_PROXY` 环境变量。
- 增加回归测试，覆盖代理端口不可用、真实不可达、直连处理器和 Windows 权限程序集加载场景。
- 补充跨电脑运行要求、架构限制、管理员权限和配置不随 EXE 携带的说明。
- 保留 v1.4 至 v0.1 的历史更新记录，并将本次工程修复、测试和发布结果记录为 v1.5。

## v1.4 · 2026-08-22

- 全面应用 XAOCEN 品牌色彩规范。
- 使用新版 `xaocen-rewifi.png` 作为本地产品介绍页主视觉。
- 统一橙黄、青靛、天蓝、青灰和中性色的使用职责。
- 更新设置窗口的背景、按钮、链接、文字和容器颜色。
- 更新本地 HTML 产品介绍页面的配色、主视觉和完整使用说明。
- 补充日志目录、配置目录、本地介绍页目录、编译和使用说明。
- 固定跨版本单实例互斥名称，避免旧版和新版同时操作 Wi‑Fi 网卡。
- 增加 GitHub 安全排除规则，避免本地配置、运行日志和构建产物进入仓库。
- 固化 win-x64 self-contained 单文件发布配置，确保 Windows 权限检测程序集随正式发布包携带。
- 修复连通性探测继承系统代理的问题，Google 和百度探测现在明确绕过系统代理与代理环境变量。
- 增加代理不可用、真实不可达和 Windows 权限程序集加载的回归测试。
- 保留 v1.3 至 v0.1 的历史更新记录，并补充 v1.4 当前版本说明。

## v1.3 · 2026-08-21

- 应用名称更新为 `XAOCEN ReWiFi`。
- 中文名称确定为“无线重连”。
- 统一应用、程序集、文件属性、托盘菜单、日志和任务计划程序版本为 `1.3`。
- 替换 EXE、快捷方式和托盘使用的 ReWiFi 圆角图标。
- 在设置窗口加入图标和自动恢复流程说明，让无主窗口的托盘程序也能直观展示工作方式。
- 新增独立产品介绍文案，统一应用介绍、版本和开发者信息。
- 设置窗口功能展示区改为完整产品介绍，并加入 GitHub 项目主页链接。
- 开机任务更名为 `XAOCEN ReWiFi`，并自动清理旧的 `XAOCEN WiFiFix` 任务。
- 自动迁移旧版本地配置，保留已设置的 SSID、网卡、等待时间和恢复选项。
- 保留实体 Wi‑Fi 网卡判断、Google/百度 HTTPS 辅助判断、恢复冷却和运行日志功能。

## v1.2 · 2026-08-15

- 完善运行日志、开机任务、配置保存和网络异常恢复流程；加入连通性探测相关功能。
- 开机任务自动创建和更新。
- 从旧版配置目录自动迁移配置。
- 解决旧版任务仍指向历史 EXE 的问题。
- 修复日志乱码问题。

## v1.1 · 2026-08-12

- Wi‑Fi 断开或无 Internet 时自动恢复。
- Wi‑Fi 已连接但 Google、百度均不可达时自动重连。
- 一个网站可达、另一个不可达时，不立即重启，避免误伤代理链路。
- 运行日志和托盘“查看运行日志”。
- 自动识别当前 SSID 和 Wi‑Fi 网卡。
- 单实例运行，防止多个后台程序同时操作网卡。

## v1.0 · 2026-08-11

- 完善运行日志、开机任务、配置保存和网络异常恢复流程；加入连通性探测相关功能。
- 诊断日志版本，增加启动路径、PID、任务路径和运行状态记录。
- 日志编码和乱码相关修正版。
- 加入 Google、百度连通性辅助判断。
- 针对网络探测、恢复流程、开机任务和稳定性进行修正。

## v0.1 · 2026-08-10

- 基础功能：托盘常驻、单实例、配置文件、管理员权限、Wi‑Fi 自动关闭/开启/重连、异常等待、恢复冷却、开机启动。

> v2.3 当前边界（2026-09-13）：ReWiFi 永久免费，账号登录可选；网络恢复不依赖账号、会员、权益或离线授权。当前客户端仅提供 XAOCEN Account 账号会话管理，历史离线授权代码和文件不会被自动删除，但不参与 v2.3 功能判断。

# XAOCEN ReWiFi 账号接入分工与联调清单

更新时间：2026-09-13

## 1. 当前产品规则

ReWiFi 只有一个 Windows x64 便携版本，不拆分在线版和离线版：

1. 不登录也可以使用全部网络监测、自动恢复、手动恢复、设置和日志功能；
2. XAOCEN Account 登录是可选能力，用于账号识别、会话恢复、退出和设备撤销；
3. 账号会话与网络恢复相互独立，账号服务不可用不会阻止核心功能；
4. v2.3 不提供会员、订阅、付费、产品权益查询、离线激活、离线授权检查或摄像头扫码；
5. 对外版本显示为 `2.3`，程序集和文件版本为 `2.3.0.0`；每次构建另写入内部构建号，用于识别同一公开版本的新构建，普通界面不显示该号码。

## 2. ReWiFi 项目负责的内容

### 2.1 当前已接入

- 使用固定参数 `productId: "rewifi"`、`platform: "windows-x64"`；
- 在线设备授权：`start → 浏览器确认 → status 轮询 → token → account/me`；
- 授权中心显示等待、批准、完成、拒绝、过期、取消和失败状态；
- 账号登录、会话恢复、刷新、退出和设备撤销；
- 访问令牌只保存在内存；
- 刷新令牌保存于 Windows Credential Manager；
- 按 Account 协议生成并登记设备公钥；设备登记密钥由 Windows Credential Manager 保护；
- 网络可用时打开官网文档，官网不可达时回退内置本地副本；用户协议和隐私说明使用相同的在线优先、失败回退本地规则；
- 首次启动先显示产品专属的用户协议和隐私说明入口，用户继续后打开产品介绍与使用帮助；协议阅读不等于同意增强分析或崩溃报告；
- 托盘与设置页使用统一的“产品介绍与使用帮助”“用户协议”“隐私说明”“报告问题”等入口；
- 可选基础统计、增强匿名分析和匿名崩溃报告，分别按隐私设置控制；
- 同一用户范围内的单实例通信：重复启动唤醒已运行实例；支持该机制的旧版可在用户确认后退出并接续启动；
- 便携包不扫描、不覆盖、不静默删除用户放在其他目录的旧 EXE；开机任务在新版本成功启动后更新到当前路径。

### 2.2 当前明确不提供

以下入口和流程已从 v2.3 用户界面及启动流程移除：

- 离线授权文件导入和本地授权有效性判断；
- `compactLicense`、离线联网检查和重新签发；
- 设备公钥二维码、授权二维码和摄像头扫描；
- ReWiFi 产品权益查询与会员/试用期限展示；
- 通过离线授权决定网络恢复是否可用。

源码中仍保留部分旧离线授权类和本地文件，以避免升级时擅自删除用户数据并保留历史兼容参考；它们不在 v2.3 启动流程、界面或遥测触发链路中使用。后续如需彻底删除这些兼容代码，应单独安排清理和回归测试。

### 2.3 本地数据与凭据

配置、日志和遥测队列位于：

```text
%LOCALAPPDATA%\XAOCEN ReWiFi\config.json
%LOCALAPPDATA%\XAOCEN ReWiFi\runtime.log
%LOCALAPPDATA%\XAOCEN ReWiFi\telemetry\state.json
%LOCALAPPDATA%\XAOCEN ReWiFi\telemetry\queue.json
```

账号刷新令牌和设备登记密钥只保存在当前 Windows 用户的 Credential Manager：

```text
XAOCEN.ReWiFi/auth.xaocen.studio
XAOCEN.ReWiFi/offline-device-key
```

访问令牌不落盘，不写入配置、日志、普通文本或遥测队列。历史版本留下的下列文件不会被本版本自动删除，也不会被 v2.3 读取：

```text
%LOCALAPPDATA%\XAOCEN ReWiFi\offline-license.xaocen-license
```

## 3. XAOCEN Account/官网项目负责的内容

- 账号登录页面、设备授权页面和授权状态自动刷新；
- Account API 的请求/响应字段、错误码、刷新、退出、撤销和设备登记协议；
- 账号数据、会话和设备管理；
- 账号官网用户协议、隐私政策及其生效页面；
- 统一反馈页面及登录、隐私同意、附件处理和问题工单；
- 遥测接收接口、去重、留存、聚合统计和后台；
- 若将来恢复会员、权益或离线授权能力，必须先更新 Account 协议，再安排独立的 ReWiFi 客户端版本，不由客户端猜测或自行补充字段。

ReWiFi 不在客户端重复实现邮箱、Google、Apple 或其他第三方登录，也不自行创建、修改或解释 Account 权益。

## 4. 当前在线账号流程

1. 用户在 ReWiFi 的“账号中心”选择登录账号；
2. 客户端调用 `POST /v1/auth/device/start`，固定传入 `rewifi` 和 `windows-x64`；
3. 客户端打开 `verificationUriComplete`，用户在 Account 官网登录并批准设备；
4. 客户端轮询 `POST /v1/auth/device/status`，授权中心显示当前轮询状态；
5. 批准后调用 `POST /v1/auth/device/token`，再调用 `GET /v1/account/me`；
6. 刷新令牌写入 Credential Manager，访问令牌只保留在内存；
7. 用户退出或撤销设备时，客户端按协议调用对应接口并清理本地刷新凭据。

浏览器标签页由系统默认浏览器管理，客户端不强制关闭标签页，也不向网页注入令牌。Account 网页负责自动刷新并显示“授权完成，可以关闭此页面”等最终状态；客户端负责在返回账号中心后显示“XAOCEN Account 已连接”。

## 5. 文档、反馈与隐私

- 产品介绍与使用帮助：在线可达时打开官网，否则打开内置本地副本；
- 用户协议和隐私说明：在线优先，官网不可达时打开内置副本；
- 报告问题：打开官网统一反馈入口，并通过 `product=rewifi` 标记产品；
- 复制脱敏诊断信息：只复制可用于排查的摘要，不自动上传完整日志；
- 基础运行统计默认启用，增强匿名分析和匿名崩溃报告独立可选；“禁止所有统计上传”会停止生成、缓存和发送统计事件；
- 当前发布包使用生产遥测入口，`TelemetryTest=true` 只用于隔离测试构建，不得用于正式分发。

## 6. 发布前验收

- 未登录时网络监测、自动恢复、手动恢复、设置保存和开机启动正常；
- 在线登录、会话恢复、刷新、退出和设备撤销正常；
- 账号服务不可达时核心网络恢复仍正常；
- 官网文档可达时打开在线页面，不可达时打开本地副本；
- 首次启动说明只出现一次，先提示协议，再打开产品介绍；
- 同一版本重复启动只保留一个进程和一个托盘图标；
- 打开同一公开版本的新构建时，显示新版路径并支持用户确认后接续；
- 基础统计、增强分析、崩溃报告和“禁止所有统计上传”分别符合隐私说明；
- 发布包为 win-x64 self-contained single-file，且不含历史二维码编译依赖；
- 当前公开版本、构建号日志、文档和发布校验值保持一致。

## 7. 外部项目待办（本项目不修改）

- Account/官网同步 ReWiFi v2.3 的永久免费、登录可选和“不提供离线授权/权益查询”的产品描述；
- 官网正式部署并校验产品页、用户协议页和隐私说明页，避免在线链接返回 404；
- 统一反馈页继续保持 `product=rewifi` 自动标记和隐私同意流程；
- Account/统计项目确认生产遥测接收、留存和聚合口径与 ReWiFi 当前文档一致。

参考协议和外部项目位置：

```text
C:\Users\TOM\Desktop\xaocen-studio\web-platform\apps\account-api\CLIENT-INTEGRATION-REWIFI.md
C:\Users\TOM\Desktop\xaocen-studio\web-platform\apps\account-api\API-PROTOCOL-1.0.md
C:\Users\TOM\Desktop\xaocen-studio\docs\ACCOUNT-TELEMETRY-CLIENT-SDK.md
```

# XAOCEN ReWiFi 授权接入分工与联调清单

更新时间：2026-09-01

## 1. 总体原则

ReWiFi 和 XAOCEN Account 使用同一个客户端版本，不拆分为“在线版”和“离线版”。授权策略如下：

1. 网络可用时，优先使用 XAOCEN Account 在线会话或在线离线授权检查；
2. Account 服务暂时不可访问时，使用本地离线签名和时间策略校验；
3. 签名无效、产品不匹配、设备不匹配、授权过期或服务端明确撤销时，不得通过回退机制掩盖；
4. v2.0 客户端使用正式账号与离线授权能力；会员、订阅、付费和权益发放仍由 XAOCEN Account 项目负责，客户端不自行创建或修改权益。

## 2. ReWiFi 项目负责的内容

### 2.1 已完成

- 在线设备授权：`start → 浏览器确认 → status 轮询 → token → account/me`；
- 在线授权中心显示“等待账号批准”“已批准”和“XAOCEN Account 已连接”等明确状态；
- 在线授权中心显示授权截止时间、上次轮询时间、下一次轮询时间、当前轮询次数和授权剩余时间；
- 访问令牌只保存在内存；
- 刷新令牌保存在 Windows Credential Manager；
- 在线会话恢复、刷新、退出和撤销；
- 在线会话建立或恢复后的 ReWiFi 产品权益查询：`GET /v1/account/entitlements`；
- 内置 `primary` 离线授权公钥；
- Ed25519 设备密钥生成；
- 设备私钥保存至 Windows Credential Manager；
- `compactLicense` 本地签名、产品、平台、设备摘要和时间策略校验；
- 离线授权字符串粘贴导入；
- `.xaocen-license` 文件导入；
- 生成设备公钥二维码，供联网手机或 Account 网页扫描；
- 扫描 Account 签发的 `compactLicense` 授权二维码；
- 扫描结果继续经过本地签名、产品、平台、设备和时间策略校验；
- 网络优先、本地回退的统一授权逻辑；
- 独立“授权中心”页面；
- 托盘提供“账号与离线授权”直达入口；
- 设置页面提供独立的“账号与离线授权”卡片，并显示在线/离线授权状态和有效期；
- 设置窗口显示独立任务栏图标，便于从其他窗口后方找回；
- 文档、反馈、脱敏诊断和运行日志在托盘与设置页面中分组折叠；
- 在线状态、离线状态和失败状态分开显示；
- 自动选择仅用于首次启动；托盘和设置页只提供在线文档、本地文档两个明确入口；
- XAOCEN 统一反馈入口和脱敏诊断信息复制入口。

“XAOCEN Account 已连接”表示本机已建立账号会话；“离线授权在线校验通过”表示离线授权文件和设备状态通过 Account 服务端检查。在线校验不会隐式建立账号会话，两个状态可以同时出现不同结果。最近一次检查结果会同步到授权中心和设置页。

客户端展示规则：账号会话、在线产品权益、离线授权文件和离线授权联网检查分别记录。设置页与授权中心均显示离线授权的 `entitlementExpiresAt`、`nextOnlineCheckAt` 和 `hardReauthorizeAt`；字段为空时显示“未设置”，不能将其误判为授权失效。

二维码识别已针对低像素电脑摄像头优化：Account 授权码采用更大的显示尺寸和静区，ReWiFi 扫描端提供灰度、二值化及放大识别回退；协议内容保持不变。

### 2.2 本地密钥和授权文件位置

离线设备私钥不是普通文件，保存于 Windows Credential Manager：

```text
Credential Target: XAOCEN.ReWiFi/offline-device-key
```

在线刷新令牌保存于：

```text
Credential Target: XAOCEN.ReWiFi/auth.xaocen.studio
```

两者都不会写入 `config.json`、运行日志或普通明文文件。

从 v1.7 到 v1.8.x，Credential Target 和 `%LOCALAPPDATA%\XAOCEN ReWiFi` 数据目录保持不变。替换 EXE 后客户端自动读取原配置和授权；导入新授权会覆盖 `offline-license.xaocen-license`，退出账号或撤销设备会删除刷新令牌。不同 Windows 用户或不同电脑不会共享这些凭据。

离线授权文件不是私钥，当前保存于：

```text
%LOCALAPPDATA%\\XAOCEN ReWiFi\\offline-license.xaocen-license
```

当前生产签名公钥 `primary` 固定内置在：

```text
src/WiFiFix/OfflineLicenseService.cs
```

### 2.3 ReWiFi 二维码能力

- 电脑端显示本机设备公钥二维码；
- 电脑端扫描 Account 返回的离线授权二维码；
- 二维码内容交由现有本地授权校验流程解析和验证；
- 二维码格式错误、授权无效、过期或类型不匹配时明确提示；
- 摄像头不可用时保留字符串粘贴和授权文件导入。
- 扫描授权二维码需要 Windows 摄像头访问权限；首次使用或权限关闭时应按提示开启，无法使用摄像头时不影响粘贴/文件导入。

当前 Account 网页实际使用的二维码内容已经确认：公钥二维码承载完整 SPKI PEM 公钥文本，授权二维码承载原始 `compactLicense` 字符串，不增加额外 JSON 包装，也不包含访问令牌或刷新令牌。

## 3. XAOCEN Account 项目负责的内容

### 3.1 浏览器授权页面

Account 网页已完成以下状态展示：

- 正在等待用户登录；
- 正在等待设备授权；
- 授权成功；
- 授权被拒绝；
- 授权已过期；
- 授权码已使用；
- 授权完成后提示“可以关闭此页面”。

当前 ReWiFi 使用系统默认浏览器打开页面，不能可靠关闭任意浏览器标签页，也不负责修改网页内容。浏览器页面的自动刷新和最终状态提示属于 Account 网页项目。

网页状态联调已完成服务端部署，ReWiFi 只需在授权中心显示本地轮询结果并引导用户返回客户端。

### 3.2 手机或网页端离线授权流程

Account 项目负责：

1. 已登录设备读取 ReWiFi 离线申请信息；
2. 调用 `/v1/account/offline/request`；
3. 调用 `/v1/account/offline-license`；
4. 展示或导出服务端签发的 `compactLicense`；
5. 支持授权文件、授权字符串或授权二维码输出；
6. 提示授权成功、失败、过期和设备数量限制。

ReWiFi 不应在客户端重复实现邮箱、Google、Apple 或其他第三方登录。

### 3.3 二维码传输约定

当前已确认的传输约定：

- ReWiFi 公钥二维码内容为完整 SPKI PEM 公钥文本；
- Account 返回的授权二维码内容为原始 `compactLicense` 字符串；
- 不把访问令牌、刷新令牌、私钥或账号密码放入二维码；
- 二维码只是传输方式，客户端仍必须执行本地签名、产品、平台、设备和时间策略校验；
- ReWiFi 继续支持复制粘贴和 `.xaocen-license` 文件导入作为备用方式。

### 3.4 统一反馈页面

Account/官网项目已完成统一反馈入口：

```text
https://www.xaocen.studio/products/support/?product=rewifi
```

页面要求登录并同意隐私说明，支持问题类型、版本、授权状态、网络状态、脱敏诊断、问题描述和可选截图。ReWiFi 只负责打开这个地址并通过 `product=rewifi` 自动标记产品。

## 4. 暂不属于本轮 ReWiFi 工作范围

以下事项已经确认交给其他项目或后续阶段：

- 安装量、活跃用户量和匿名统计：暂缓到账号本地/在线验证完成后再规划；
- 会员、订阅、付费和权益发放/修改：由 Account 项目负责；ReWiFi 仅查询并展示 `rewifi` 权益，不在客户端创建或修改权益。

统一反馈页面已上线，ReWiFi 当前反馈按钮已切换到该入口；GitHub Issues 不再作为客户端默认反馈入口。

## 5. ReWiFi 真机联调流程

### 5.0 当前联调前置状态

- ReWiFi `v2.0` 正式版本已完成界面、Account 权益接口和离线授权能力接入，并通过 Release 编译检查（0 警告、0 错误）。
- 当前正式发布版本为 `2.0.0.0`；二维码、低像素摄像头、断网回退和在线恢复均已完成真机验收。
- Account 页面已核对权益有效，ReWiFi 现在可以进入在线授权、权益查询和离线授权联调。
- v2.0 发布包：`build/v2.0-publish/XAOCEN.ReWiFi.exe`；文件版本 `2.0.0.0`，大小 `92,229,983` 字节，SHA-256：`70BCA7368495F12DB01ED891AEF8B32DBC672E65C6A5CCEF0B2C4D3567F403B4`。当前目标框架为 `net10.0-windows`，继续使用自包含单文件和 OpenCV 摄像头扫码。

### 5.1 在线账号授权

1. 打开“账号与离线授权”；
2. 在设置页点击“打开授权中心”，再点击“在线授权”；
3. 浏览器完成 XAOCEN Account 登录和设备批准；
4. 返回 ReWiFi 授权中心；
5. 确认页面显示“XAOCEN Account 已连接”；授权等待期间应显示“等待账号批准”、上次/下一次轮询时间、轮询次数和授权剩余时间；
6. 关闭并重新启动 ReWiFi，确认会话可以恢复；
7. 测试“退出账号”和“撤销此设备”。

Account 浏览器页面已经支持自动刷新，并在授权完成、拒绝、过期或授权码使用后显示对应结果；授权完成后用户可以关闭浏览器。

### 5.2 本地文档

本地产品介绍页面位置：

```text
%LOCALAPPDATA%\\XAOCEN ReWiFi\\XAOCEN-ReWiFi-介绍.html
```

测试入口：

- 设置窗口 → 帮助与文档 → 在线文档或本地文档；
- 托盘菜单 → 文档与帮助 → 在线文档或本地文档；
- 首次启动时自动选择；
- 网络不可用时首次启动回退本地文档，之后可手动打开本地文档；网络恢复后可手动打开在线文档。

### 5.3 离线授权

1. 在授权中心点击“显示公钥二维码”，或点击“复制设备公钥”；
2. 由已登录 XAOCEN Account 的联网设备扫描公钥并申请离线授权；
3. Account 签发授权后，在网页显示授权二维码；
4. 在 ReWiFi 授权中心点击“扫描授权二维码”，或使用授权文件/字符串导入；
5. 确认本地签名和设备校验通过；
6. 点击“检查当前授权”；
7. 断开网络或阻断 Account 域名，确认客户端自动回退到本地校验；
8. 恢复网络后再次检查，确认在线校验和状态恢复正常。

### 5.4 需要 Account 项目继续配合的联调

- 授权二维码过期和重复使用；
- Account 端撤销离线设备后，ReWiFi 在线检查拒绝授权。

## 6. 正式发布后的维护事项

- Account 项目负责生产环境协议、授权网页、权益配置和离线授权服务的持续维护；
- 客户端只使用正式协议，不在本地保存账号令牌或私钥明文；
- 新增协议字段、错误码、公钥轮换或二维码格式时，需由 Account 项目先更新协议文档，再安排客户端版本升级；
- 安装量、活跃用户量和匿名统计仍属于后续独立项目，不随 v2.0 默认启用。

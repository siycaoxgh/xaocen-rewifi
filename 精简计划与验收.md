# v2.3 便携版精简与升级验收

日期：2026-09-14

## 已完成

- 永久免费、登录可选：网络恢复不依赖登录或会员。
- 设置页和托盘改为账号中心，删除权益、离线授权和摄像头扫码入口。
- 停止启动离线校验以及登录/恢复会话后的权益请求。
- 保留登录、刷新、退出和协议要求的设备公钥登记；删除当前产品不再使用的撤销适配，其他设备管理和账号注销由 Account 网页提供。
- 发布不再依赖 OpenCV、QRCoder；历史二维码、离线授权和离线签名源码均从编译排除，Git 中仍可查阅。
- 保留 BouncyCastle：现行 Account 协议的稳定 Ed25519 设备密钥仍需要它；设备身份由独立的 `DeviceIdentityService` 管理。
- 对外版本统一为 2.3，文件与程序集版本为 2.3.0.0；自包含单文件便携包，无需安装 .NET。
- 沿用配置、账号凭据和统计偏好；未删除用户历史离线文件或设备密钥。
- 增加同一用户范围内的单实例通信：重复启动会唤醒现有窗口，新版可请求支持该协议的旧版正常退出并接续启动。
- 更新流程只处理进程协调与开机任务路径，不扫描、不删除桌面、下载目录或其他位置的旧 EXE。
- 退出时先停止网络监听、释放账号对象并限时刷新统计，最后隐藏托盘；正常退出超过 10 秒时结束自身，避免无托盘图标的残留进程继续占用单实例锁。
- 文档入口统一为“产品介绍与使用帮助”；首次说明和协议按钮采用同一套在线优先、不可达回退本地规则。
- 内置产品介绍、用户协议和隐私说明统一页面框架，保留内容差异但统一字体、颜色、卡片、边距和页脚布局。
- 每次编译和发布自动写入程序集隐藏构建号；单实例交接在公开版本相同的情况下继续比较该构建号，避免同为 2.3 时误判为同一文件。
- 正式发布构建中的增强匿名分析、匿名崩溃报告和禁止所有统计上传均可正常选择，不再错误依赖测试构建标志。
- 账号登录轮询支持显式取消；切换浏览器账号时先取消旧请求即可重新登录，关闭账号中心也会终止轮询。
- 设置窗口和账号中心默认使用 1040×900，允许调整大小和最大化；按 DPI 缩放，超出屏幕工作区时自动缩小，内容不足时显示滚动条。
- 账号中心采用应用级单例；设置页和托盘入口均复用已有窗口。“打开账号中心”收回账号会话卡片，设置页剩余高度优先用于完整显示隐私说明。
- 两个窗口使用最小内容高度和独立滚动视口；纵向缩小时显示右侧滚动条，不再压缩或隐藏底部按钮和文字。
- 产品帮助、用户协议和隐私说明共用一致的在线优先判断和 6 秒超时；在线检查失败时分别回退对应的内置副本。
- 用户协议和隐私说明顶部统一为与产品介绍相同的纯白卡片背景，不再使用两套不同渐变。
- 恢复流程在取消、退出或升级接续时会执行独立的网卡重新启用兜底，并终止仍在运行的 `netsh` 子进程。
- 账号退出不再依赖服务端成功响应：网络异常时仍清理本地刷新凭据，并同步刷新账号中心与设置页。
- 开机任务状态按任务是否实际存在判断；更新页面指向 GitHub Releases；脱敏诊断包含内部构建号。

## 验证与交付

- Release 编译通过，0 警告、0 错误；普通便携发布成功。
- 发布依赖清单无 OpenCV 和 QRCoder。
- v2.3 正式便携包 `build/v2.3-release-20260914-r7/XAOCEN-ReWiFi.exe`：56,555,844 字节（约 53.94 MiB），产品版本 2.3，文件版本 2.3.0.0，SHA-256 为 `B01D0FE74CEBE6FFD498ECB73D49D2FDC503F9C50F0D3ED4FBA11C6933A7A855`。该包使用 `TelemetryTest=false`，内部构建号为 `639249667889753231`。
- 账号中心已通过实际 WinForms 渲染及控件边界检查：登录、取消登录和退出账号按钮高度均为 36 像素，操作区可用高度为 56 像素，未发生越界或裁切。
- v2.0.1 精简包曾为 56,554,824 字节，作为历史体积基准；对应历史构建目录已清理。
- 对比上一个统一 UI 包 92,271,455 字节，减少 35,716,631 字节，约 38.7%。
- 普通发布包不启用历史遥测测试地址，使用生产统计接收地址。
- 未结束用户当前运行的旧进程，未改写项目外的 Account/官网文件；本次交付会将源码和项目文档提交到 ReWiFi GitHub 仓库，构建输出仍按规范不上传。
- 已完成编译、发布、单文件内容和版本元数据检查；因当前旧版正在运行，未自动执行会关闭旧版的交互测试，单实例升级流程保留给真机验收。

## 真机验证步骤

1. 在 2.0.2 或更早版本运行时打开 2.3，确认前台提示先退出旧版；旧版退出后重新打开新包。
2. 在 2.3 运行时再次打开同一文件，确认现有窗口被唤醒且不会出现第二个托盘实例。
3. 后续测试更高版本时，确认更新对话框显示新旧版本和新版路径；选择继续后旧版退出、新版自动接续启动。
4. 选择退出后观察任务管理器，确认旧进程最迟在 10 秒内消失，且不会出现“托盘已消失但进程仍存在”的状态。
5. 未登录时确认网络监测、设置保存、手动恢复和开机启动可用。
6. 打开账号中心登录、退出；确认无需会员或激活，并观察原账号会话恢复。
7. 核对反馈跳转：官网浏览器会话与客户端会话独立，不向 URL 注入令牌。
8. 对比托盘空闲、设置打开及关闭后的工作集/私有内存；当前不承诺具体内存降幅。
9. 在正式包设置页切换增强分析、崩溃报告和禁止所有上传，保存后重新打开设置，确认选择保持一致。
10. 发起账号登录后在浏览器退出并切换账号；点击“取消登录”，确认状态恢复，再重新发起登录并完成连接。
11. 分别在 100%、125% 和 150% 显示缩放下打开两个窗口，调整窗口大小并最大化，确认标题、按钮、复选框和说明均可见；小窗口应出现滚动条而不是裁切。
12. 从设置页打开账号中心后，再点击托盘“账号中心”，确认只置顶已有窗口且系统中始终只有一个账号中心。
13. 将设置页和账号中心分别纵向缩至最小高度，确认右侧滚动条出现，且滚动后能看到全部账号按钮、隐私说明和保存/关闭按钮。
14. 在官网可达时分别打开产品帮助、用户协议和隐私说明，确认三者均打开浏览器；断网后重复操作，确认三者分别打开对应本地副本。

## 其他项目配合事项

- Account 如计划让免费产品登录不再登记权益设备，应提供更新协议；客户端当前保留登记兼容。
- 官网需同步永久免费、登录可选、v2.3 下载地址及便携版更新说明；本次仅修改 ReWiFi 项目。
- 正式构建已固定使用生产遥测入口；Account/统计项目仍需负责生产接收、留存和聚合口径。

## 参考链接

- ReWiFi 项目仓库：https://github.com/siycaoxgh/xaocen-rewifi
- XAOCEN ReWiFi 官网产品页：https://www.xaocen.studio/products/rewifi/
- XAOCEN 统一支持入口：https://www.xaocen.studio/products/support/
- Account API 协议和遥测 SDK 由 XAOCEN Account 项目维护；当前没有确认的公开文档地址，不在本项目文档中写入本地磁盘路径。

> v2.3 当前规则（2026-09-14）：普通发布包使用生产遥测入口。基础运行统计默认启用；增强匿名分析和匿名崩溃报告独立可选且默认关闭；“禁止所有统计上传”会停止生成、缓存和发送遥测事件。

# XAOCEN ReWiFi 遥测接入与隐私说明

更新时间：2026-09-14

## 1. 当前状态

ReWiFi 已接入 XAOCEN Telemetry API 1.0。v2.3 正式便携包使用生产接收地址；只有明确使用 `TelemetryTest=true` 编译的隔离测试包才会使用测试地址。

正式接收地址：

```text
https://auth.xaocen.studio/v1/telemetry/events
```

隔离测试接收地址：

```text
https://telemetry-test.xaocen.studio/v1/telemetry/events
```

当前发布构建为 `TelemetryTest=false`，不得把测试构建用于正式分发。遥测不要求 XAOCEN Account 登录，也不携带 Account 访问令牌。

## 2. 用户选择

遥测分为三类，互不代替：

- 基础运行统计：默认启用，用于统计首次运行、启动、版本更新和核心功能启用规模；
- 增强匿名分析：单独可选，默认关闭，用于有限的功能使用和会话汇总；
- 匿名崩溃报告：单独可选，默认关闭，用于经脱敏的异常信息。

首次启动说明先展示用户协议和隐私说明；用户可以在首次说明中禁止所有统计上传，也可以在设置页修改选择。阅读产品介绍或登录账号不等于同意增强分析或崩溃报告。

“禁止所有统计上传”是总开关。启用后会取消客户端中正在进行的遥测请求和退避重试，不再创建、缓存或发送遥测事件，并清理本地待上传队列和可能生成事件的待处理时间；在用户操作前已经由服务端接收的请求不能撤回。以后重新允许上传时，只从重新允许的时间开始观察，不补传禁止期间的首次运行、更新、激活或其他活动。关闭增强分析或崩溃报告时，会删除对应尚未发送的事件。v2.3 正式版设置页允许直接修改这三个选项；只有总开关处于启用状态时，增强分析和崩溃报告才会暂时变为不可选。

## 3. 数据范围

所有事件固定使用：

```text
productId: rewifi
platform: windows-x64
schemaVersion: 1.0
```

协议要求的最小事件信封可能包含：

- 随机 UUIDv4 `eventId`；
- 匿名持久化 `instanceId`；
- 每次启动更新的 `sessionId`；
- 事件名称和发生时间；
- 产品编号、平台和公开应用版本；
- 当前统计选择和隐私政策版本。

基础事件不包含 Windows 版本、处理器架构、网卡名称、SSID、MAC、IP、日志正文、崩溃堆栈或授权内容。增强事件和崩溃事件也只能使用协议规定的白名单字段，并在上传前执行脱敏和长度限制。

客户端不会通过遥测上传：

- 邮箱、Account 用户编号或其他账号标识；
- 访问令牌、刷新令牌、激活码、授权字符串、离线授权文件内容或私钥；
- Wi-Fi 名称、网卡名称、MAC、硬件序列号、用户名或文件内容；
- 原始 IP、完整运行日志、剪贴板内容或未经脱敏的崩溃转储。

网络请求本身仍可能被服务端或网络基础设施看到来源 IP、时间等连接层信息；“遥测事件不上传 IP”不等于所有网络请求绝对匿名。

## 4. 当前事件

| 事件 | 选择 | 用途 |
| --- | --- | --- |
| `app.first_run` | 基础统计 | 首次观察到匿名实例 |
| `app.start` | 基础统计 | 记录一次启动 |
| `app.updated` | 基础统计 | 记录公开版本变化 |
| `activation.success` | 基础统计 | 记录核心网络监测成功启用 |
| `app.session_summary` | 增强分析 | 正常退出时的会话汇总 |
| `feature.used` | 增强分析 | 设置、账号中心或手动恢复等功能使用 |
| `activation.failure` | 增强分析 | 核心启动失败摘要 |
| `crash.reported` | 崩溃报告 | 用户同意后上传脱敏异常摘要 |
| `telemetry.opt_out` | 对应选择 | 记录撤回增强或崩溃选择；禁止全部上传时不发送 |

便携 EXE 没有安装器，“首次运行规模”只能作为便携实例首次运行的近似统计，不等同于准确安装人数、卸载人数或自然人用户数。

## 5. 本地保存

遥测状态与队列保存在：

```text
%LOCALAPPDATA%\XAOCEN ReWiFi\telemetry\state.json
%LOCALAPPDATA%\XAOCEN ReWiFi\telemetry\queue.json
```

- `state.json` 保存匿名实例、会话选择和版本观察信息；
- `queue.json` 保存暂时未发送的匿名事件；
- 不保存 Account 访问令牌、刷新令牌或私钥；
- 队列最多 500 条，单次最多发送 100 条；
- 用户重置匿名标识时会删除本地队列并生成新标识；
- 升级替换 EXE 不会自动删除遥测状态，历史选择继续沿用。

网络不可用时，遥测不会阻塞 ReWiFi 的启动、网络监测或恢复；事件可暂存在队列，网络恢复后按协议重试。服务端按照 `eventId` 幂等去重。

## 6. 崩溃脱敏

客户端会尝试处理异常消息中的 Bearer Token、路径、邮箱、IPv4/IPv6、MAC、SSID、网卡名称等敏感值，并限制字段长度。不保证捕获强制终止、系统崩溃或所有原生 DLL 崩溃，也不上传原始内存转储和完整运行日志。

## 7. 测试与验证

源码构建：

```powershell
dotnet build .\XAOCEN-WiFiFix.sln -c Release --no-restore
```

隔离测试构建必须显式使用：

```powershell
dotnet publish .\src\WiFiFix\WiFiFix.csproj -c Release -r win-x64 --self-contained true -p:TelemetryTest=true -o .\build\telemetry-test
```

正式构建不得使用 `TelemetryTest=true`。测试至少覆盖：基础事件上传、增强分析未同意时不生成、崩溃报告默认关闭、禁止所有上传、断网排队、恢复重试、重复事件去重、队列清理和遥测失败不影响 Wi-Fi 恢复。

## 8. 项目分工

ReWiFi 负责同意控制、本地状态、事件触发、脱敏、队列、重试和客户端回归测试。

Account/统计项目负责接收接口、鉴权边界、去重、留存、删除请求、聚合统计、后台、生产部署和官网隐私政策。生产规则变化时，应先更新 Account 协议和隐私政策，再安排 ReWiFi 客户端版本更新。

# XAOCEN ReWiFi 遥测接入与隐私说明

更新时间：2026-09-04

## 1. 当前状态

ReWiFi 已完成 XAOCEN Telemetry API 1.0 的 C# 客户端适配，但当前只允许测试环境接入。普通 v2.0 发布构建不启用测试遥测地址，生产遥测服务尚未发布前不得切换正式入口。

测试接收地址：

```text
https://telemetry-test.xaocen.studio/v1/telemetry/events
```

测试构建通过 MSBuild 属性 `TelemetryTest=true` 显式启用。没有该属性时，客户端不会向测试遥测服务发送数据。

## 2. 用户同意

匿名使用统计和匿名崩溃报告是两个独立选项，默认均为关闭。统计设置位于 ReWiFi 设置窗口的“隐私与统计（测试）”区域。

- 用户不同意统计时，不创建普通统计事件；
- 用户不同意崩溃报告时，不创建 `crash.reported` 事件；
- 关闭统计会删除对应的待上传事件；
- 用户可以重置匿名实例 ID并删除本地待上传数据；
- 关闭或拒绝统计不会影响 Wi-Fi 恢复、XAOCEN Account 登录、账号授权、权益查询或离线授权；
- 当前测试接入不要求 Account 登录，不携带 Account 访问令牌。

正式发布前，需要由 Account/统计项目提供正式隐私政策地址、同意文案、生产入口和数据删除规则。

## 3. 上传数据

客户端使用固定产品参数：

```text
productId: rewifi
platform: windows-x64
schemaVersion: 1.0
```

事件信封包含：

- 随机 UUIDv4 `eventId`；
- 匿名持久化 `instanceId`；
- 每次启动更新的 `sessionId`；
- 事件名称和发生时间；
- 产品编号、平台、客户端版本；
- Windows 版本和处理器架构；
- 测试/发布通道；
- 当前统计同意状态和隐私政策版本；
- 事件专属的白名单属性。

事件属性只允许使用 Account 协议定义的字段。客户端不会上传：

- 邮箱、Account 用户编号、OAuth 编号；
- 访问令牌、刷新令牌、激活码、授权字符串；
- 离线授权私钥、公钥全文或授权文件内容；
- SSID、网卡名称、MAC、硬件序列号、用户名和文件内容；
- 原始 IP、完整运行日志、剪贴板或未经脱敏的崩溃转储。

## 4. 事件目录

当前客户端使用以下事件：

| 事件 | 用途 | 触发时机 |
| --- | --- | --- |
| `app.first_run` | 首次运行规模 | 首次观察到实例，并在用户同意统计后发送 |
| `app.updated` | 版本更新 | 检测到上次版本与当前版本不同，并在用户同意统计后发送 |
| `app.start` | 活跃实例 | 每次启动且用户已同意统计 |
| `app.session_summary` | 会话汇总 | 正常退出时，按时间区间汇总 |
| `feature.used` | 功能使用 | 打开设置、打开账号中心或手动恢复 |
| `activation.success` | 核心功能激活 | 当前实例首次成功启动网络监控 |
| `activation.failure` | 激活失败 | 预留给核心启动失败场景 |
| `entitlement.checked` | 授权检查汇总 | 在线/离线授权导入或检查完成 |
| `crash.reported` | 崩溃汇总 | 用户已同意崩溃报告且捕获到异常 |
| `telemetry.opt_out` | 同意撤回 | 用户关闭统计或崩溃报告 |

“安装量”在便携式 EXE 模式下按首次运行实例统计，不等同于安装器安装次数。若以后需要准确统计安装/卸载，需要单独引入安装器。

## 5. 本地数据位置

遥测数据与 ReWiFi 配置、离线授权文件分开保存：

```text
%LOCALAPPDATA%\XAOCEN ReWiFi\telemetry\state.json
%LOCALAPPDATA%\XAOCEN ReWiFi\telemetry\queue.json
```

其中：

- `state.json` 保存匿名实例、会话同意状态和版本观察信息；
- `queue.json` 保存尚未发送的匿名事件；
- 不保存 Account 访问令牌、刷新令牌或离线设备私钥；
- 队列最多保留 500 条事件；
- 单次最多发送 100 条事件；
- 用户可在设置中重置实例并删除队列。

## 6. 发送与重试

- 网络不可用时不阻塞 ReWiFi 启动和核心功能，事件保存在本地队列；
- HTTP 201 后移除当前批次，服务端按 `eventId` 幂等去重；
- 400 类字段错误不自动重试；
- 413 时拆分批次；
- 408、429 和 5xx 使用有限次数退避重试；
- 429 优先使用 `Retry-After`；
- 连续失败后保留队列，下一次启动或后续事件触发时继续尝试；
- 遥测服务不可用不会改变授权状态，也不会触发 Wi-Fi 恢复逻辑。

## 7. 崩溃脱敏

客户端捕获 WinForms UI 异常、未处理异常和未观察后台任务异常。上传前会处理：

- Bearer Token；
- Windows/Linux 用户路径；
- 邮箱；
- IPv4/IPv6；
- MAC 地址；
- 当前配置中的 SSID 和网卡名称；
- 字段长度限制。

当前不保证捕获强制终止、系统崩溃或所有原生 DLL 崩溃。`native` 字段仅保留协议兼容性，暂不接入 Windows Error Reporting。

## 8. 测试构建

普通构建：

```powershell
dotnet build .\XAOCEN-WiFiFix.sln -c Release --no-restore
```

遥测测试构建：

```powershell
dotnet publish .\src\WiFiFix\WiFiFix.csproj -c Release -r win-x64 --self-contained true -p:TelemetryTest=true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -p:DebugType=None -o .\build\v2.0-telemetry-test
```

当前测试包：

```text
build\v2.0-telemetry-test\XAOCEN.ReWiFi.exe
文件版本：2.0.0.0
大小：92,266,847 字节
SHA-256：79536F51E0DF23F7805886F7FCEDF97B3C6AA15F313599B3361DFA7A56AB546E
```

测试前应验证：

1. 未同意前不产生普通事件；
2. 同意统计后产生首次运行和启动事件；
3. 关闭统计后删除普通统计队列；
4. 崩溃报告未单独同意时不上传；
5. 断网时事件进入队列；
6. 恢复网络后批量发送；
7. 重复 `eventId` 不造成重复计数；
8. 413、429、5xx 均按协议处理；
9. 授权、离线校验和 Wi-Fi 恢复不受遥测失败影响；
10. 升级客户端后匿名实例继续保留；
11. 重置匿名实例后生成新 ID并删除队列；
12. 测试后台能看到 ReWiFi 聚合数据。

## 9. 项目边界

ReWiFi 项目负责客户端适配、同意控制、本地存储、事件触发、脱敏、重试和回归测试。

Account/统计项目负责测试/生产接收接口、数据库、事件去重、隐私政策、数据删除、聚合后台、公开统计和生产环境发布。

生产遥测地址正式发布并完成验收前，ReWiFi 不应把测试构建升级为正式统计版本。

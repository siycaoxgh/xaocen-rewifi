# XAOCEN ReWiFi 项目状态与归档说明

更新时间：2026-09-15

## 1. 当前定位

XAOCEN ReWiFi（无线重连）是 Windows x64 Wi-Fi 自动恢复托盘工具。程序读取 Windows 已保存的网络配置和当前连接状态，在指定 Wi-Fi 持续异常时自动禁用、重新启用网卡并连接已保存的 Wi-Fi Profile。

v2.4 是当前 Windows x64 便携版基线：永久免费，账号登录可选；不登录也可以使用全部网络监测、自动恢复、手动恢复、设置和日志功能。XAOCEN Account 只用于可选账号识别、会话恢复和退出，不参与核心网络恢复授权；其他设备由 Account 网页管理。

当前版本不提供会员、订阅、付费、产品权益查询、离线激活、离线授权检查或摄像头扫码。源码中保留的旧离线授权与二维码文件已从 v2.4 编译清单排除；本地遗留授权文件不会被启动流程读取，也不会被自动删除。

发布者：**XAOCEN STUDIO**  
中文名称：**无线重连**
当前公开版本：**v2.4**

## 2. 当前功能

- Windows 10 / Windows 11 后台托盘运行；
- 稳定的单实例锁和跨版本启动协调；
- 开机自动启动；
- 自动识别当前连接的 SSID 和 Wi-Fi 网卡；
- 按配置周期读取本地网络状态；
- 异常持续指定时间后触发恢复；
- Wi-Fi 网卡关闭、开启和重新连接；
- Google / 百度 HTTPS 连通性辅助判断；
- 一个可达、一个不可达时不立即重启，减少代理环境误判；
- 自动恢复冷却，避免恢复流程无限循环；
- 托盘状态、立即恢复、设置和运行日志；
- 首次启动先提示用户协议和隐私说明，继续后打开产品介绍与使用帮助；在线可用时优先打开官网，失败时回退内置本地页面；
- 可选 XAOCEN Account 登录、会话恢复、刷新和退出；服务端不可达时仍可完成本机退出；
- 基础运行统计默认启用，增强匿名分析和匿名崩溃报告独立可选，并提供禁止全部上传的总开关；
- 账号登录轮询可取消，切换浏览器账号时可终止旧请求后重新登录；正式版隐私选项可在设置页直接修改；
- 设置窗口和账号中心默认使用 1040×900，支持调整大小、最大化、DPI 自动缩放、屏幕工作区适配和滚动回退；
- 账号中心是应用级单例，设置页和托盘入口只会复用并置顶同一个窗口；
- 设置页和账号中心采用独立纵向滚动视口，缩小窗口时不会通过压缩内容来隐藏底部控件；
- 产品帮助、用户协议和隐私说明统一使用 6 秒在线检查，官网可达时打开浏览器，不可达时打开各自内置副本；
- win-x64 self-contained single-file，目标电脑无需另外安装 .NET。
- 恢复流程被退出或升级打断时执行网卡重新启用兜底，避免 Wi-Fi 停留在禁用状态；
- 开机任务状态与实际任务存在状态保持一致；复制脱敏诊断会包含内部构建号。
- Wi-Fi 自动识别按接口 GUID 匹配系统无线网卡，兼容本地化命令输出；无法可靠区分多块网卡时停止自动填写而不是猜测。
- 配置采用原子替换保存；任务计划命令具有超时；账号会话通过不可变引用在后台和界面线程间发布。
- 禁止全部统计上传期间不保留可供以后补传的首次运行、更新或激活状态。
- 禁止全部统计上传时会取消正在发送或重试的遥测批次；异常成功响应不会让后台任务失控或误删队列。
- Account 新刷新令牌先写入 Credential Manager，再执行账号资料请求和兼容登记。
- 网络状态优先通过 Network List Manager 原生接口读取，PowerShell 只作为失败回退；开机任务使用四态结果区分禁用、不存在和查询错误。
- 网络监测通过 Windows WLAN 原生接口核对当前连接 SSID；连接其他 Wi-Fi 不再被误判为目标网络正常。
- 外网探测响应程序退出取消；托盘与设置页配置保存失败时回滚原配置和开机任务状态。
- 单实例更新确认最长等待 30 秒并提供明确超时提示。

程序不保存 Wi-Fi 密码，不管理代理软件，不提供测速。恢复操作需要管理员权限，开机启动使用 Windows 任务计划程序。

## 3. 核心判断逻辑

网络检测不是通过测速或 Ping 完成，而是综合 Windows 本地网络状态和轻量 HTTPS 探测：

- Wi-Fi 网卡不存在、被禁用、SSID 消失或物理断开：进入恢复流程；
- Wi-Fi 已连接到配置的目标 SSID，且 Windows 状态正常：继续监听；
- Wi-Fi 已连接但不是配置的目标 SSID：等待异常确认时间后恢复目标网络；
- Windows 状态异常：等待配置的异常确认时间；
- Google 和百度都可达：认为外网正常，不恢复；
- 一个可达、一个不可达：认为可能是代理规则或代理链路问题，不恢复；
- 两个都不可达：认为实体 Wi-Fi 可能异常，进入恢复流程；
- 恢复完成后进入冷却时间，冷却期间不重复触发自动恢复。

程序不会尝试修改代理软件，也不能判断代理链路内部的具体节点状态。

## 4. 源码结构

主要源码位于 `src/WiFiFix/`：

| 文件 | 职责 |
| --- | --- |
| `Program.cs` | 程序入口、管理员权限、单实例、配置加载、账号会话和退出处理 |
| `AppConfig.cs` | 本地 JSON 配置、保存、默认值和旧配置迁移 |
| `AppLogger.cs` | 运行日志和内部构建号记录 |
| `NetworkWatcher.cs` | 网络轮询、异常确认、探测判断、冷却和恢复触发 |
| `ConnectivityProbe.cs` | Google / 百度 HTTPS 辅助探测 |
| `WifiController.cs` | 调用 Windows 命令关闭、开启和连接 Wi-Fi |
| `StartupManager.cs` | Windows 任务计划开机启动 |
| `TrayManager.cs` | 托盘图标、菜单、状态和通知 |
| `SettingsForm.cs` | 设置窗口、网络配置、账号摘要、隐私设置和帮助入口 |
| `AuthorizationForm.cs` | 可选 XAOCEN Account 登录、取消登录、退出和状态界面 |
| `AccountClient.cs` | Account 设备授权、资料、刷新、退出和设备登记客户端 |
| `DeviceIdentityService.cs` | 生成并复用 Account 设备登记所需的稳定 Ed25519 公钥 |
| `TelemetryClient.cs` | 统计同意、事件、脱敏、队列和重试 |
| `DocumentationRouter.cs` | 在线文档可达性判断和本地回退 |
| `AboutPage.cs` | 生成并打开内置产品介绍、用户协议和隐私说明 |
| `SingleInstanceCoordinator.cs` | 重复启动唤醒、版本比较和新版接续 |
| `WiFiFix.csproj` | .NET 10 WinForms、版本、资源和发布配置 |

旧二维码源码和旧离线授权实现仅作历史兼容参考，均不纳入 v2.4 发布编译。

## 5. 本地数据与升级

当前用户数据目录：

```text
%LOCALAPPDATA%\XAOCEN ReWiFi\
```

常用文件：

```text
config.json
runtime.log
telemetry\state.json
telemetry\queue.json
XAOCEN-ReWiFi-介绍.html
XAOCEN-ReWiFi-terms.html
XAOCEN-ReWiFi-privacy.html
```

`config.json` 只保存目标 SSID、网卡名称、等待时间、冷却时间、网络开关和首次说明状态，不保存 Wi-Fi 密码。统计偏好和匿名状态保存在 `telemetry\state.json`，待上传队列保存在 `telemetry\queue.json`。账号刷新令牌和设备登记密钥保存在当前 Windows 用户的 Credential Manager；访问令牌只保存在内存。

升级替换 EXE 不会清除配置、日志、统计偏好或账号凭据。支持新通信协议的旧版可在用户确认后退出，让新版自动接续；2.0.2 及更早版本没有该通信通道，首次升级时需要从旧版托盘退出后再启动新版。便携版不会扫描或静默删除其他目录的旧 EXE，旧文件由用户确认新版本正常后自行处理。

每次构建会写入程序集内部构建号。公开界面只显示 `2.4`，单实例更新协调还会比较内部构建号，以识别同一公开版本的重新构建文件。

## 6. 编译与发布

开发环境：

- Windows 10 / Windows 11；
- .NET 10 SDK；
- WinForms；
- win-x64。

普通编译：

```powershell
dotnet build .\XAOCEN-WiFiFix.sln -c Release --no-restore
```

正式便携发布：

```powershell
dotnet publish .\src\WiFiFix\WiFiFix.csproj -c Release -r win-x64 --self-contained true -p:TelemetryTest=false -o .\build\v2.4-release
```

2026-09-15 当前逻辑复查正式输出文件：

```text
build\v2.4-release-20260915-r1\XAOCEN-ReWiFi.exe
```

该包文件版本为 `2.4.0.0`，大小 `56,565,572` 字节，SHA-256 为 `47A4EE73D455B11D72150E0C471468098D04EDBEF35BF27BA8A8FCC5062312E5`，内部构建号为 `639250004422715460`。历史构建已清理，`build` 目录只保留此 Windows x64 正式包。

测试遥测构建必须使用独立目录，并且不得分发：

```powershell
dotnet publish .\src\WiFiFix\WiFiFix.csproj -c Release -r win-x64 --self-contained true -p:TelemetryTest=true -o .\build\telemetry-test
```

仓库不保存构建输出、运行日志、用户配置、令牌、授权文件或签名私钥。

## 7. GitHub 位置

主项目仓库：

```text
https://github.com/siycaoxgh/xaocen-rewifi
```

历史归档仓库：

```text
https://github.com/siycaoxgh/xaocen-rewifi-archive
```

主项目仓库保存源码、文档、内置页面和当前图标；历史构建输出仅在本地清理，不上传到主仓库。

## 8. 后续维护边界

- ReWiFi 项目负责 Wi-Fi 恢复、客户端账号入口、同意控制、遥测适配、文档回退和单实例升级；
- Account/官网项目负责账号服务、在线页面、官网协议、统一反馈和遥测后台；
- Account/官网规则发生变化时，先更新外部协议与网页，再更新 ReWiFi 客户端；
- 不要把本地 `config.json`、`runtime.log`、Credential Manager 内容或任何授权私钥提交到 GitHub；
- 如果未来需要安装量、卸载量或准确安装器统计，应另行开发安装器，不把便携 EXE 的首次运行数当成精确安装数。

## 9. 历史说明

v0.1 至 v1.5 的网络恢复演进、v1.6/v1.7 的账号与离线授权测试、v2.0 的统一授权界面和 v2.3 的便携版精简记录，统一保留在 `更新日志.md`。其中历史版本的离线授权、权益和二维码记录不代表当前 v2.4 功能。

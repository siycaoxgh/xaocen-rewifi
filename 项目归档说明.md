# XAOCEN ReWiFi 项目归档说明

## 1. 项目定位

XAOCEN ReWiFi（中文名：无线重连）是一个 Windows Wi-Fi 自动恢复托盘工具。

程序常驻 Windows 托盘，读取 Windows 当前维护的 Wi-Fi 和网络状态。当指定 Wi-Fi 持续异常时，程序会自动：

1. 关闭 Wi-Fi 网卡；
2. 等待 2 秒；
3. 重新开启 Wi-Fi 网卡；
4. 等待 3 秒；
5. 连接 Windows 已保存的目标 Wi-Fi Profile；
6. 进入冷却时间并恢复后台监听。

程序不保存 Wi-Fi 密码，不管理代理软件，不连接自有服务器，也不提供测速、账号、云服务或自动更新功能。

当前归档版本：**v1.5**  
发布者：**XAOCEN STUDIO**  
中文名称：**无线重连**

本项目目前处于稳定归档状态。除非收到明确的 Bug 反馈或出现兼容性问题，暂不计划主动增加功能。

## 2. 当前功能

- Windows 10 / Windows 11 后台托盘运行；
- 程序单实例运行；
- 开机自动启动；
- 自动识别当前连接的 SSID 和 Wi-Fi 网卡；
- 每 2 秒读取本地网络状态；
- 异常持续指定时间后再触发恢复；
- Wi-Fi 网卡关闭、开启和重新连接；
- Google / 百度 HTTPS 连通性辅助判断；
- 一个可达、一个不可达时不立即重启，减少代理环境下的误判；
- 自动恢复冷却，避免恢复流程无限循环；
- 托盘状态显示、立即恢复、设置和运行日志；
- 首次运行自动打开本地产品介绍页面；
- 使用 Windows 已保存的 Wi-Fi Profile，不提供密码输入框。
- 正式发布固定为 win-x64 self-contained single-file，并保留 Windows 权限检测程序集。
- 连通性探测明确绕过系统代理和 HTTP_PROXY、HTTPS_PROXY、ALL_PROXY 环境变量。
- v1.5 已补充 Windows 权限程序集、代理绕过和真实断网场景的回归测试。

## 3. 核心判断逻辑

网络检测不是通过测速或 Ping 完成，而是综合 Windows 本地网络状态和轻量 HTTPS 探测：

- Wi-Fi 网卡不存在、被禁用、SSID 消失或物理断开：进入恢复流程；
- Wi-Fi 已连接且 Windows 状态正常：继续监听；
- Windows 状态异常：等待配置的异常确认时间；
- Google 和百度都可达：认为外网正常，不恢复；
- 一个可达、一个不可达：认为可能是代理规则或代理链路问题，不恢复；
- 两个都不可达：认为实体 Wi-Fi 可能异常，进入恢复流程；
- 恢复完成后进入冷却时间，冷却期间不重复触发自动恢复。

程序不会尝试修改代理软件，也不能判断代理链路内部的具体节点状态。

## 4. 源码结构

项目主要位于 src/WiFiFix/，没有复杂的第三方依赖。

| 文件 | 职责 |
| --- | --- |
| Program.cs | 程序入口、管理员权限、稳定的跨版本单实例互斥、配置加载和退出处理 |
| AppConfig.cs | 本地 JSON 配置读取、保存、默认值和旧配置迁移 |
| AppLogger.cs | 运行日志写入 |
| NetworkWatcher.cs | 网络状态轮询、异常确认、探测结果判断、冷却和恢复触发 |
| ConnectivityProbe.cs | Google / 百度 HTTPS 连通性辅助探测 |
| WifiController.cs | 调用 Windows 自带命令关闭、开启和连接 Wi-Fi |
| StartupManager.cs | Windows 任务计划程序开机启动管理 |
| TrayManager.cs | 托盘图标、菜单、状态文字和通知 |
| SettingsForm.cs | 设置窗口、自动识别当前 Wi-Fi、品牌配色和控件布局 |
| AboutPage.cs | 生成并打开本地产品介绍 HTML 页面 |
| ProductInfo.cs | 产品名称、版本、GitHub 地址等统一信息 |
| WiFiFix.csproj | .NET 10 WinForms 项目配置、版本信息和资源嵌入 |
| app.manifest | Windows 管理员权限和应用清单 |

回归测试位于 `tests/WiFiFix.Tests/`，覆盖直连探测、代理环境隔离、真实不可达判断和 Windows 权限程序集加载。

## 5. 本地数据位置

程序运行后使用当前 Windows 用户的本地应用数据目录：

    %LOCALAPPDATA%\XAOCEN ReWiFi\

实际常用文件：

    %LOCALAPPDATA%\XAOCEN ReWiFi\config.json
    %LOCALAPPDATA%\XAOCEN ReWiFi\runtime.log
    %LOCALAPPDATA%\XAOCEN ReWiFi\XAOCEN-ReWiFi-介绍.html
    %LOCALAPPDATA%\XAOCEN ReWiFi\xaocen-rewifi.png

配置中只保存 SSID、网卡名称、等待时间、冷却时间和开关选项，不保存 Wi-Fi 密码。

如果需要重新测试首次介绍页，将 config.json 中的 welcomeShown 从 true 改为 false。

## 6. 编译和发布

开发环境：

- Windows 10 / Windows 11；
- .NET 10 SDK；
- WinForms；
- win-x64。

普通编译：

    dotnet build .\XAOCEN-WiFiFix.sln -c Release

回归测试：

    dotnet test .\XAOCEN-WiFiFix.sln -c Release

独立单文件发布：

    dotnet publish .\src\WiFiFix\WiFiFix.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o .\src\WiFiFix\bin\Release\v1.5-publish

当前发布文件：

    src\WiFiFix\bin\Release\v1.5-publish\XAOCEN.ReWiFi.exe

独立 EXE 约 57 MB，目标电脑不需要另外安装 .NET 10。

## 7. GitHub 位置

主项目仓库：

    https://github.com/siycaoxgh/xaocen-rewifi

历史归档仓库：

    https://github.com/siycaoxgh/xaocen-rewifi-archive

主项目仓库保存源码、文档和当前图标，不保存构建输出、运行日志、用户配置或签名私钥。

归档仓库保存旧构建依赖和旧设计素材，但明确排除所有 .exe 文件、用户数据、日志和签名私钥。

## 8. 未来重新维护时的建议顺序

如果一两年后重新启动项目，建议按下面顺序检查：

1. 先确认 .NET 10 SDK、Windows 版本和 Wi-Fi 命令仍兼容；
2. 检查 runtime.log，确认问题属于 Windows Wi-Fi、代理链路还是探测误判；
3. 检查 NetworkWatcher.cs 和 ConnectivityProbe.cs 的判断逻辑；
4. 检查 WifiController.cs 中的网卡启用、禁用和连接命令；
5. 检查 StartupManager.cs 是否仍能创建最高权限任务计划；
6. 修改版本号时同步检查 WiFiFix.csproj、app.manifest、ProductInfo.cs、README、更新日志和本地 HTML；
7. 先进行框架依赖编译，再进行独立单文件发布；
8. 不要把本地 config.json、runtime.log、令牌、密码或签名私钥提交到 GitHub。

## 9. 适合扩展的方向

如果未来确实需要增加功能，建议优先考虑：

- 更清晰的网络状态和恢复原因日志；
- 可配置探测地址和探测超时；
- 失败次数统计与通知；
- Windows 事件日志或更完善的故障诊断；
- 对不同 Wi-Fi 网卡和 Windows 版本的兼容性处理；
- 发布版数字签名或 Microsoft Store MSIX 发布。

不建议重新引入密码管理、代理自动化、云端服务、账号系统、测速评分或复杂后台服务，以免偏离项目原本的轻量定位。

## 10. 历史版本

完整历史记录保存在：

- 根目录 更新日志.md；
- README.md 的“更新日志”章节；
- 本地产品介绍页面的“更新日志”章节。

当前项目已经完成 v0.1 至 v1.5 的主要功能演进，并以 v1.5 作为当前稳定归档版本；v1.4 及更早版本的历史记录继续保留。
